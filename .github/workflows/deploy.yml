name: Deploy to EC2 via SSM (Docker)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::696661408242:role/GitHub-SSM-Deploy-Role
          aws-region: eu-north-1

      - name: 🚀 Deploy via SSM
        run: |
          echo "🚀 Sending SSM deploy command..."

          COMMAND_ID=$(aws ssm send-command \
            --document-name "AWS-RunShellScript" \
            --targets "Key=tag:App,Values=django-blog" \
            --comment "Deploy Docker Django App" \
            --parameters 'commands=[
              "set -e",
              "sudo -u ubuntu git config --global --add safe.directory /home/ubuntu/mydjangoproject/Blog_Project",
              "cd /home/ubuntu/mydjangoproject/Blog_Project",
              "echo Current directory: $(pwd)",
              "sudo -u ubuntu git pull origin main",
              "sudo docker compose down",
              "sudo docker compose up -d --build"
            ]' \
            --query "Command.CommandId" \
            --output text)

          echo "📌 SSM Command ID: $COMMAND_ID"
          echo "⏳ Waiting for command to complete..."

          INSTANCE_ID=$(aws ssm list-command-invocations \
            --command-id $COMMAND_ID \
            --query "CommandInvocations[0].InstanceId" \
            --output text)

          aws ssm wait command-executed \
            --command-id $COMMAND_ID \
            --instance-id $INSTANCE_ID

          echo "✅ Deployment completed successfully!"
